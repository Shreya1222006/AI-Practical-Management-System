Services scaffold in `services/`:

- api-gateway
- auth-service
- submission-service
- execution-runner
- file-service

Each service contains a minimal scaffold (`package.json`, `src/index.ts`, `src/app.ts`). Start services individually during development with `npm run dev` in the service folder (install dependencies first).
